import { UpdateProfileResponse } from './update-profile-response';

describe('UpdateProfileResponse', () => {
  it('should create an instance', () => {
    expect(new UpdateProfileResponse()).toBeTruthy();
  });
});
